<!-- provenance: schema=1 project=the-factory(working-label) session=e2b0ba9f-40d2-4bee-aa18-685ce2c8f428 actor=amodal1 agent=anthropic/claude-fable-5 generated_at=2026-08-21 status=review -->

> **Assumes:** `docs/design/03-card-schema.md` **draft-5** (2026-08-21, rounds 49–58 as ruled), `03a`, `03b`, the round-5 review, and the three preserved prototypes (`review-artifacts/2026-08-20-round3/r3-impl/`, `…round4/r4-impl/`, `…round5/r5-impl/`).
> **Descends from:** owner direction round 22 (loop until dry); the handoff of 2026-08-21 (round 6 = the narrowest; fresh agents, never forks); the owner's standing rules — *deterministic functions, literally*; *efficiency of construction and execution*; *"not now" is never "never"*; Rust/Python/TypeScript targets.
> **Method (the narrowest, as planned):** **I4** a fourth clean-room implementer building from draft-5 alone (`write` + recompute table + predicate, `ratify(ids)` as one transaction with the policy-chain `batch-manifest`, re-ratification, the governed-document grammar over four document types, the ten verbs, the events fold, per-commit reconciliation), then running r3/r4/r5 as oracles and classifying every disagreement as *ruled* or *underspecified* (artifacts `review-artifacts/2026-08-21-round6/r6-impl/`, findings `r6-findings.md`); **E2** the bloat re-count — the nine operations of 9.4 re-derived from the text, then round 5's three sweeps (stored twice · detected after prevented · pays for itself) re-run, a vocabulary census and a v1 construction count (`r6-bloat.md`). Refuter ids kept (I4-Bn, E2-Bn); owner calls are X-series.

# Adversarial review — card schema draft-5 (2026-08-21)

> **Owner rulings 2026-08-21 (round 7ba, one call per turn):** **X1** — *"b with the display pin. next"* — `ratify(writes: [WriteRequest])`, `ratify(ids)` as the shorthand, the dry run and the signer listing tending and hold changes since the last signed entry. **X2** — after asking *"if we do A, then what happens if the merge to main happens and the scond write event for state.json etc never happens"* — *"A with the three pins."*: the store lands on `main` after the PR merges (merge commit = cursor); `closed (pending-land)` + a queue line "merged, not landed: N"; the cursor does not advance and the tenant's next batch does not dispatch while a land is pending; the factory observes the merge through ingest. First bridge target confirmed: sartor. **Store per tenant** (7ba.6: *"b, one store per tenant. next"*). Applied as draft-6; **closing check clean** (220/220, `../../review-artifacts/2026-08-21-round6/r6-closing-check.md`; C1–C7 applied) — **the schema is complete in first draft** (sync record 7bb).

## 0. Verdict in one paragraph

**The core is still dry, and the periphery is now buildable from the document alone.** Four independent hashers (r3, r4, r5, r6) agree byte-for-byte on build and claims hashes across the whole G4 corpus; chain links and single/batch signature bytes agree wherever draft-5 did not deliberately change them, and **all 49 oracle disagreements are explained by a named ruling** (`sig`/`batch` outside the chain content — W2; `sign(tenant ‖ h)` — V2; the footer — round 26; `scope_mark` hashed — U8/W9; the prefixed `h` entering the chain — 5.5) except one, which is a pin (NFC before the `[extensions]` key lookup). The implementer built every mechanism section 11 named for this round — 153/153 checks; the one-write-one-entry rule, the re-ratification row, the predicate (the re-kind-to-`watching` trick is refused), the manifest, the page document type with no special-casing, the byte-perfect hand-written line caught by reconciliation — all hold. The bloat re-count confirms the subtraction: a card's lifetime derives to **3 store calls of its own + 2/N + 1/batch and 7 card-side hashes** (9.4's ≈ 3 / ≈ 7 hold) plus 4 journal-chain hashes and 3/N batch hashes that 9.4 does not count; six small duplicate copies remain and the outer-gate vocabulary reduces to W9's two reasons by one pin (the gates run `write`'s derive function *including its refusals*). **Round 6 is not dry by the stated test — two findings are shape-changing — but both sit on the same kind of seam: a sentence in 1.12 and a sentence in 1.3 promise something the function signature next to them cannot deliver.** (1) *What rides the sitting's batch*: hold releases, answers and re-ratifications are document changes, and `ratify(ids)` takes ids; (2) *where the land commits*: governed diffs are `main`-only in 1.3 and exempted on the batch PR in 1.14. Each is one owner call with a recommendation. Everything else — 17 implementer pins, 28 bloat pins, a v1 sequencing list with every seam named — is fill. **Round 7 should not be needed: apply the two rulings and the pins as draft-6 and record the schema complete in first draft, with one implementer re-run of the r6 scenarios against draft-6 as the closing check.**

## 1. Shape-changing calls — for the owner (X-series)

| # | Call | Raised by | Recommendation |
|---|---|---|---|
| X1 | **What rides the sitting's batch.** Draft-5 says "Ratifications and hold releases for those cards ride the same batch" (1.12 step 3), "exceptions ride the next sitting's batch" (step 5) and an answer is "a gated change → re-ratification at the next sitting" (1.11) — but `ratify(ids, dry_run)` takes ids only, and a release, an answer, an `accepted` (needs `ref`), an `unwithdrawn` and a `--as guidance` edit each change a *document*. As written each of those is an owner `write` whose signer runs inside the call: **one un-cacheable act per exception**, against round 42/50's one signature per sitting. Three readings: **(a)** the batch signs status flips only; every other signed act is its own signature — N acts per sitting. **(b)** `ratify(writes: [WriteRequest])` with `WriteRequest = {path, document, base, ref}` — the sitting's batch **is** a list of writes under one signature; `ratify(ids)` is sugar for "these, `status → ratified`, otherwise unchanged"; the planner composes a release, a guidance edit, an answer or a reopen as a request; the per-entry `act` still comes from the recompute table; the dry run diffs the same documents the sitting signs (r6 built this). **(c)** no new structure: the contributor demotes (`ratified → draft`, unsigned), edits the draft (clearing the hold — "anything on a draft by anyone", 1.8), and the sitting ratifies `draft → ratified` — one batch-signed `ratified` entry. **Verified cost of (c):** the signer's display reads `card · ratified · build`; the blocked-hold clearance never appears as `released` — the Andon asymmetry (T4) is satisfied by the owner's signature, but the owner is not shown what was released. | I4-B1 · E2-B3 | **owner — recommend (b)**, plus one display pin under any reading: for every member that has a prior signed entry, the dry-run and signer display lists the **tending and hold changes since the last signed entry** beside the gated diff (1.11 already shows removed questions and new answers first) — so a demoted-and-cleared hold is shown as a release whichever path it took. Cost of (b): one list-typed parameter and a second content path into `write`; nothing else. |
| X2 | **Where the land commits.** 1.3: "CI refuses governed-path diffs on any branch but `main`"; 1.14: "batch-PR CI fails on any tracking-root change *other than the sidecar, `BOARD.md` and inbox intake*"; 1.4: "the batch PR shows every landed change in one diff". Both cannot hold. **Choice A — the store lands the sidecar, inbox intake and board on `main` directly**, as its ordinary commit, **after** the batch PR merges (the merge commit is the cursor; the PR merge stays the forge's button — round 14); the batch PR carries code only; strike the 1.14 exemption; 1.4's "one diff" becomes the land commit. Cost: the owner reads two diffs per batch (the code PR, then the land commit); the sidecar lags the merge by one commit. **Choice B — a branch-aware store** writing governed paths on the batch branch: a second write target, journal rows keyed by ref, and governed files from two refs meeting at the PR merge — the merge-driver problem W4 removed. | E2-B1 | **owner — recommend A.** Touches the catalog's rolling-branch and land rows (T-C3, T-A10): land = a post-merge store write on `main` with the merge commit as cursor. |

## 2. Fill worklist for draft-6 (mechanics; no ruling needed)

**Pins from the implementer (I4-B2–B18)** — a contributor's retraction (`closed → ratified` with only `closures[-1].retracted: false → true` on an un-landed closure) is exempt from the "more active" clause; the owner's `closed`/`withdrawn` are signed by a caller-aware clause (owner + status to closed/withdrawn ⇒ sign) — recompute unaffected; `write.no-change` iff `D == []` and `ref` is `None`; the store fills `ref` for the owner's `closed` from `closures[-1]` of `after`; the closure-entry hash bytes = `sha256(canonical_json(closure))` with no prefix lines, `retracted` present; **NFC before the `[extensions]` key lookup** (both sides; a declaration not NFC-unique is `ext-schema.key-collision`) — the one corpus pair where the oracles split; the `batch-manifest` entry's keys (`by` = the signer's principal, `fields = []`, `build` = the policy build hash, `sig` = the one batch signature — see E2-B8); `build` on policy-chain entries = the 5.1 construction over `config.toml` minus `[history]`, or dropped from policy entries — pick one; the realm binding as `act = "binding"` (policy file only) with `until` absent while open, never null; the non-card geneses use the document's own registry schema version; the `page:` genesis ident = the governed path; the array key under `[history]` = `entries`; a repairing journal row carries `sig = sign(tenant ‖ h_row)` outside `c` and `repairs = <commit>` inside; record-slot documents (the inbox) skip steps 2–4 of `write` and ignore `base`; `blocked ↔ deferred` re-kind = `held`, unsigned; the scalars' mutual order in an emitted head = the 2.1 inventory order (blob ids must agree across stores); `D` uses the same canonical JSON for tending keys (an NFD→NFC retype of `summary` is no change). Also from the run: `questions.dropped-unanswered` as written binds drafts too — bind it to cards with a prior signed entry, so a planner can delete its own typo question on a draft; a refused creation burns an id (allocation precedes validation) — harmless, state it.

**Pins from the bloat re-count (E2-B2, B4–B15, B19–B23, B29, B31, B35, B37)** — `GovernedPath::NewCard{slug}`: `write(NewCard, document, None, None)` allocates the id inside the call and returns `{id, path, head}`; `write` never runs `id.unique` (the counter is authoritative), `check` does; **one derive function at every gate, refusals included** — `derive(before, after, ref) -> Result<Entry, RuleId>`, an `Err` at a gate is `integrity:tampered`; then `log.rewritten`, `questions.dropped-unanswered` and the append-only rules on `closures`/`reopens`/`see`/extension lists live once, inside `write` (prerequisite: `write` runs the append-only diff on every structured call, not only `--set updates+=`), and the outer vocabulary loses `log-rewritten`, `claims-drift` (row 3 retires into row 1), `deletion`; **the hook that recomputes is gone** — strike it from 1.2, 1.7, 1.15, 9.2 (9.6 is right: the hook is one check); rules declare their input keys and `write` runs those whose inputs intersect `D` (a tending edit does not re-resolve `refs` or re-walk the graph; whole-set rules run when `D` touches a relation key and always under `ratify`/`check`); dry-run build hashes = 0 on a warm blob cache, N cold; `ratify` never re-hashes a blob the dry run verified; 9.4 adds the manifest's policy `h` and the journal row's `h`; `show <target>` with `Target = Card(id) | Board | Queue | Inbox | Schema(name@version)` — the five referenced-but-unlisted verbs fold, ten stay ten; the batch `sig` once on the manifest entry, members carry `batch` only, `sig` on an entry iff singly signed; drop journal `h_prev` (the chain binds it); the ratifier pin in one home (`[ratification].pin`); drop `state.json.tenant` or name it the lander's assertion; drop `runs[].surfaces_declared`; redefine 5.2 as the closure-entry hash (the whole-set `claims_hash` has no consumer after W6) and delete it from 1.7/1.9; strike "`Co-Authored-By` beside `author` in the fingerprint"; one `Origin` enum with per-schema allowed subsets for the two `source` enums; sidecar event kinds that share a card act's name are the ledger's own transition carrying the entry's `seq`, never its content; `ref.drifted` / `ref-drifted` one name (kebab, an event); `closure-invalidated` = `reopened (pending-ingest)`; remove the same-day Updates amend (the seam: a later block carrying `corrects = "#u<n>"`); the board manifest row is grant-only and `board.commit` the one key; the profile-verdict ids lose the section number (`profile.story.acceptance`); "the canonical clone of the tracking root" → the repo (the store resolves `refs` outside the root and commits to `main`); state the per-write commit + push in 9.4.

**Verified, priced, kept (E2-B16–B18, B24–B25, B27, B33, B36, B38–B39)** — journal (caller, at) beside entry (by, at) is the two-witness design (round 30 / round 24), ≈ 80 bytes per write; `history_head`/`journal_head` are pointers; the run-report copy of suggestions is the zero-trust double (round 27); `unratified`, `time`, `attribution` are ≈ 0-cost belts for the reclassification, tarball and non-git cases (say in 1.15 that the latter two never fire alone when the journal is present); the five `amended`-equivalent acts kept for grep-ability (round 57); three chains one function; every owner-verbatim mechanism priced and required.

## 3. What the refuters verified and could not break

Four hashers agree on build and claims bytes across CRLF, key order, NFC values, comments, tending edits and footer appends (190 agreements, 49 disagreements, 0 unclassified); one write = one entry (`draft → ratified` with a content change is one signed `ratified` carrying the new `build`); re-ratification derives from row 3 and the parent-blob → commit-blob recompute matches with one entry per card per commit including the demote/re-promote path; the sitting is 1 signer call, 1 journal row, 1 commit, one `at` for the N entries and the manifest, each member verifying through the manifest with `sig`/`batch` stripped from `h`; the predicate refuses the re-kind-to-`watching` trick and a contributor's clear on a ratified card, allows `watching` clears and any clear on a draft; `questions.dropped-unanswered` is refused inside `write`; `accepted` carries `D == []` and a typed `ref`, appended as a line insertion with the file's prefix bytes unchanged; the claims hash never enters `build`; a tampered footer byte is `tampered`, a consistent rewrite of the whole chain is `unverified` and `rewritten`; `repair --history` chains from `h_k` with covered entries marked; reconciliation explains every store write, flags a byte-perfect hand-written line with no row, and accepts `repair --journal`; the page document type runs through the card's code with no branch on the type; parse → emit → parse is a fixed point; the ten verbs reach all 18 acts.

## 4. v1 build sequence — deferrable with the seam named [proposed; not a shape call]

From the construction count (E2 section 4: 11 store functions, 15 pure functions, 6 document types, 4 registry schemas, ≈ 30 enums, 5 signer backends, 4 runners, 3 chains + journal). Nothing the owner's words require is in this column; each row names the seam it keeps open:

| Deferrable | Seam |
|---|---|
| `tpm-hello`, `yubikey`, `signal-approve` — `remote-totp` (the portable default, round 58) + `software_key_ack` first; **the Windows TPM the owner named (round 32) is second in sequence, not dropped** | the closed `SignerBackend` enum and the one `alg:key_fpr:base64` format fixed now; one module per backend |
| `http` runner; `ears` and `classic` shapes; `sprint`/`milestone` profiles; `[x]` | the closed enums keep every member; `profiles.enabled`; `head.unknown-key` rejects `x` and `ext_schema_hash = sha256("")` when absent |
| id renumber repair + the genesis switch (a duplicate on `main` is a bypass — `unjournaled`, repaired by `repair --journal` + a fresh card) | `renumbered_from` reserved; 5.5's sentence; returns with a bulk import |
| `ratification.mode = "declared"` (a software key always can exist; `software_key_ack` covers it) | the `mode` enum |
| `grants` as a config table; board per-segment caps as config; `inbox.max_per_actor_per_day` | isidium G7; `board.*`; the config key |
| the realm binding record (the `init` pin verifies until the realm exists) | the `(key_fpr, at)` lookup has two sources: pin now, binding entries when G7 lands |
| the `.feature` projection | a renderer over the same compile output |
| the neighborhood projection, `constraint.check`, `manual_attestation` — needed at **first dispatch**, not during bridge authoring: sequence, not deferral | 1.17, 4.4, 1.13 stand |

## 5. Next

Owner rules X1 and X2 (one per turn) → **draft-6** (the two rulings + section 2 applied; a small pass) → one implementer re-run of the r6 scenarios against draft-6 as the closing check (the prototype is built; the re-run is cheap) → **schema complete in first draft** → the config-schema doc (`04-config-schema.md`), the catalog follow-ups (now including T-C3/T-A10 for X2), the per-phase roster, the bridge sessions.
