"""The core: pure functions, no I/O. canon (5.1–5.3), chain (5.5–5.6), grammar (9.1, 03b §3),
derive (1.2: diff · recompute table · append-only · the signature predicate), validate (section 3, 1.11),
config (04). Every refusal is a `Refusal` carrying a rule id — the `Result<_, RuleId>` of the Rust port."""
