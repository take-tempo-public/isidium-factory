"""The clients (03b §2): the tenant's thin client and its one pre-commit hook, the factory's lander client, the
planner's tool surface, the owner's CLI. Every one of them talks to the store over the channel pinned in the tenant
registration; none of them writes a governed path itself."""
